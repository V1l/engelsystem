


<IfModule mod_rewrite.c>
    RewriteEngine on

	RewriteCond %{HTTPS} !=on 
	RewriteRule ^(.*) https://%{SERVER_NAME}/helfer/$1 [R,L] 
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>
